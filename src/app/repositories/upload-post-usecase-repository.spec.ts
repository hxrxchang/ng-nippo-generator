import { UploadPostUsecaseRepository } from './upload-post-usecase-repository';

describe('UploadPostUsecaseRepository', () => {
  it('should create an instance', () => {
    expect(new UploadPostUsecaseRepository()).toBeTruthy();
  });
});
