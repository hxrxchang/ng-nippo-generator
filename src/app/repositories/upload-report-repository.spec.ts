import { UploadReportRepository } from './upload-report-repository';

describe('UploadReportRepository', () => {
  it('should create an instance', () => {
    expect(new UploadReportRepository()).toBeTruthy();
  });
});
